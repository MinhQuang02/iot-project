services:
  - type: web
    name: django-mqtt-bridge
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn IotServer.wsgi --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
